# Commit Guidelines

We follow structured commits:

format:
type(scope): message

Types:
- feat: new feature
- fix: bug fix
- test: tests
- refactor: internal changes
- chore: tooling/ci

Examples:
feat(protocol): add request parsing
fix(store): correct delete semantics
test(handler): add GET tests